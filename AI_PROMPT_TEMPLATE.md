# AI 代码解释生成提示词

将下面代码块中的内容发送给能够读写项目文件的 AI 编程助手。如果只需要解释部分模块，先替换其中的「整个项目」扫描范围。

```text
你现在位于项目根目录。请通读整个项目中所有编程语言的源码，先识别每种语言的语法、工程结构和惯用法，再理解模块边界、类型与函数的职责、调用链、数据流向和业务上下文，最后为源码生成中文逐行解释。

请按下述完整协议创建或更新 IHM 旁注文件。IHM 是与源码逐行对应的 UTF-8 纯文本文件：第 N 行文本是源文件第 N 行的解释，空行表示该行不显示解释。你不需要预先了解、安装或调用任何 IDEA 插件，只需严格按以下路径映射和逐行对齐规则生成文件。不要把解释写进源码，也不要修改任何源码。

文件映射规则：
1. 对项目根目录内的每个可阅读源代码文件，在项目根目录的 inlay-hints 中完整保留其相对于项目根目录的路径。源码可以直接位于项目根目录，也可以位于 src 或其他任意层级目录；不要处理 inlay-hints 目录自身。按项目实际使用的语言处理，不限于某一种源文件扩展名。
2. IHM 文件名为完整源文件名后追加 .ihm。例如，项目根目录的 main.go 对应 inlay-hints/main.go.ihm，src/Demo.java 对应 inlay-hints/src/Demo.java.ihm，module/src/Worker.kt 对应 inlay-hints/module/src/Worker.kt.ihm。
3. IHM 必须使用 UTF-8 编码。源文件的每一行必须与 IHM 的同一行严格对应，不得增加额外的标题、列表、代码块或换行。
4. 源码空行对应 IHM 空行。每个源文件处理完后，必须校验源文件与 IHM 的行数和行位置完全一致。

解释规则：
1. 所有解释使用通俗、准确的中文，每行最多一条解释。
2. 纯粹用于声明包、命名空间或模块的行无需解释，对应 IHM 行保持空白。请按当前语言识别这类语句，例如 package、namespace 或同等语义的其他写法，不要只匹配固定关键词。
3. 纯粹用于导入或引用依赖的行无需解释，对应 IHM 行保持空白。请按当前语言识别 import、from ... import、using、use、include、require 或其他等价语句；只有当整行的作用确实只是引入依赖时才跳过。
4. 整行只有注释时无需解释，对应 IHM 行必须保持空白，不要为已有注释额外添加 hint。此规则包括单行注释、文档注释、块注释的正文行，以及只包含块注释起止符的行；如果同一行同时包含有效代码和行尾注释，则仍按该行代码的语义决定是否解释。
5. 只用于结束代码块且不承载其他语义的行无需解释，对应 IHM 行保持空白。请根据语言识别 }、};、end、fi、done 或其他等价的块结束写法；缩进定义代码块的语言不需要虚构结束行。
6. 不要只把当前行翻译成中文。必须结合当前类型或源文件、所属模块、实现关系、调用方和业务流程，说明该行在整体链路中的作用。
7. 对当前语言中的类型、数据结构和可调用单元声明，例如类、接口、结构体、trait、枚举、函数、方法、过程或闭包，说明它们的职责以及与其他组件的关系。
8. 对关键条件、状态变化、资源或对象的创建与释放、异步调用、定时任务和错误处理，优先解释其业务原因和对后续流程的影响。
9. 只写能够从源码和项目上下文验证的内容；无法确认时不要编造业务含义。
10. 不要在 IHM 中写 Markdown 标题、序号或“第 X 行”等无关前缀。

执行方式：
1. 先识别项目使用的所有编程语言和各自的源码边界，再分析项目结构和跨语言、跨模块调用关系，最后分模块生成 IHM。不要只阅读单个文件就开始猜测。
2. 可以分批处理，但必须覆盖约定范围内的全部源码。
3. 生成结束后逐文件检查映射路径、UTF-8 编码和行数。
4. 最后汇报已处理的模块数、源文件数、IHM 文件数以及校验结果。
```
